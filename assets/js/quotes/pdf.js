(function (root, factory) {
  const api = factory();
  if (typeof module === 'object' && module.exports) module.exports = api;
  else root.DMQuotePdf = api;
}(typeof self !== 'undefined' ? self : this, function () {
  'use strict';
  const cleanFile = value => String(value || 'cotizacion').normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/[^a-z0-9_-]+/gi, '-').replace(/-+/g, '-').replace(/^-|-$/g, '').slice(0, 70) || 'cotizacion';
  function generate(quote, totals, calculations) {
    if (!globalThis.jspdf?.jsPDF) throw new Error('El generador de PDF no está disponible.');
    const { jsPDF } = globalThis.jspdf; const doc = new jsPDF({ unit: 'mm', format: 'a4', compress: true });
    const left = 16, right = 194, width = right - left; let y = 18; const currency = quote.currency;
    const money = value => calculations.formatMoney(value, currency); const safe = value => String(value || '');
    const pageCheck = needed => { if (y + needed > 278) { doc.addPage(); y = 18; } };
    doc.setFillColor(7, 61, 58); doc.rect(0, 0, 210, 8, 'F');
    if (quote.business.logoDataUrl) { try { const type = quote.business.logoDataUrl.startsWith('data:image/png') ? 'PNG' : quote.business.logoDataUrl.startsWith('data:image/webp') ? 'WEBP' : 'JPEG'; doc.addImage(quote.business.logoDataUrl, type, left, y, 24, 18, undefined, 'FAST'); } catch {} }
    const textLeft = quote.business.logoDataUrl ? 44 : left;
    doc.setTextColor(5, 45, 43); doc.setFont('helvetica', 'bold'); doc.setFontSize(16); doc.text(safe(quote.business.name || 'Tu negocio'), textLeft, y + 5);
    doc.setFont('helvetica', 'normal'); doc.setTextColor(82, 98, 95); doc.setFontSize(8.5); const businessLines = [quote.business.contact, quote.business.phone, quote.business.email, [quote.business.address, quote.business.city, quote.business.country].filter(Boolean).join(', '), quote.business.taxId ? `Identificación fiscal: ${quote.business.taxId}` : '', quote.business.website].filter(Boolean); doc.text(businessLines, textLeft, y + 10);
    doc.setTextColor(217, 119, 6); doc.setFont('helvetica', 'bold'); doc.setFontSize(10); doc.text('COTIZACIÓN', right, y + 2, { align: 'right' }); doc.setTextColor(5, 45, 43); doc.setFontSize(13); doc.text(safe(quote.quoteNumber), right, y + 8, { align: 'right' }); doc.setFontSize(8); doc.setFont('helvetica', 'normal'); doc.text(safe(quote.status).toUpperCase(), right, y + 13, { align: 'right' }); y += Math.max(28, 12 + businessLines.length * 4);
    doc.setDrawColor(7, 61, 58); doc.setLineWidth(.6); doc.line(left, y, right, y); y += 7;
    doc.setFillColor(245, 248, 247); doc.roundedRect(left, y, width, 18, 2, 2, 'F'); doc.setTextColor(82, 98, 95); doc.setFontSize(7); doc.text('FECHA', left + 5, y + 5); doc.text('VÁLIDA HASTA', left + 62, y + 5); doc.text('MONEDA', left + 124, y + 5); doc.setTextColor(5, 45, 43); doc.setFontSize(9); doc.setFont('helvetica', 'bold'); doc.text(safe(quote.issueDate), left + 5, y + 12); doc.text(safe(quote.expiryDate), left + 62, y + 12); doc.text(safe(currency), left + 124, y + 12); y += 26;
    doc.setFont('helvetica', 'normal'); doc.setTextColor(82, 98, 95); doc.setFontSize(7); doc.text('COTIZACIÓN PARA', left, y); y += 5; doc.setFont('helvetica', 'bold'); doc.setTextColor(5, 45, 43); doc.setFontSize(11); doc.text(safe(quote.customer.name), left, y); y += 4; doc.setFont('helvetica', 'normal'); doc.setTextColor(82, 98, 95); doc.setFontSize(8); const customerLines = [quote.customer.company, quote.customer.phone, quote.customer.email, [quote.customer.address, quote.customer.city, quote.customer.country].filter(Boolean).join(', ')].filter(Boolean); if (customerLines.length) { doc.text(customerLines, left, y + 2); y += customerLines.length * 4 + 6; } else y += 4;
    const columns = [left, left + 86, left + 105, left + 130, left + 148, right];
    pageCheck(16); doc.setFillColor(7, 61, 58); doc.rect(left, y, width, 8, 'F'); doc.setTextColor(255); doc.setFont('helvetica', 'bold'); doc.setFontSize(7); doc.text('Descripción', columns[0] + 2, y + 5); doc.text('Cant.', columns[1], y + 5, { align: 'right' }); doc.text('Precio', columns[2], y + 5, { align: 'right' }); doc.text('Desc.', columns[3], y + 5, { align: 'right' }); doc.text('Imp.', columns[4], y + 5, { align: 'right' }); doc.text('Total', columns[5] - 2, y + 5, { align: 'right' }); y += 8;
    totals.lines.forEach(line => { const description = safe(line.item.description || 'Partida'); const wrapped = doc.splitTextToSize(description, 78); const height = Math.max(9, wrapped.length * 4 + 3); pageCheck(height + 2); doc.setTextColor(37, 50, 47); doc.setFont('helvetica', 'normal'); doc.setFontSize(7.5); doc.text(wrapped, columns[0] + 2, y + 5); doc.text(String(line.quantity), columns[1], y + 5, { align: 'right' }); doc.text(money(line.unitPrice), columns[2], y + 5, { align: 'right' }); doc.text(`${line.discountRate}%`, columns[3], y + 5, { align: 'right' }); doc.text(`${line.taxRate}%`, columns[4], y + 5, { align: 'right' }); doc.text(money(line.total), columns[5] - 2, y + 5, { align: 'right' }); doc.setDrawColor(220, 228, 226); doc.line(left, y + height, right, y + height); y += height; });
    pageCheck(38); y += 5; const sx = 126; doc.setFontSize(8); [['Subtotal', totals.subtotal], ['Descuento', -totals.discount], ['Impuesto', totals.tax]].forEach(([label, value]) => { doc.setTextColor(82, 98, 95); doc.text(label, sx, y); doc.setTextColor(5, 45, 43); doc.text(money(value), right, y, { align: 'right' }); y += 6; }); doc.setFillColor(7, 61, 58); doc.rect(sx - 4, y - 3.5, right - sx + 4, 10, 'F'); doc.setTextColor(255); doc.setFont('helvetica', 'bold'); doc.setFontSize(10); doc.text('Total', sx, y + 2.5); doc.text(money(totals.total), right - 2, y + 2.5, { align: 'right' }); y += 15;
    const addSection = (title, content) => { if (!content) return; const wrapped = doc.splitTextToSize(safe(content), width); pageCheck(10 + wrapped.length * 4); doc.setFont('helvetica', 'bold'); doc.setTextColor(5, 45, 43); doc.setFontSize(9); doc.text(title, left, y); y += 5; doc.setFont('helvetica', 'normal'); doc.setTextColor(82, 98, 95); doc.setFontSize(8); doc.text(wrapped, left, y); y += wrapped.length * 4 + 7; };
    addSection('Notas', quote.notes); addSection('Términos y condiciones', quote.terms); pageCheck(12); doc.setDrawColor(220, 228, 226); doc.line(left, y, right, y); y += 6; doc.setTextColor(82, 98, 95); doc.setFontSize(7.5); doc.text('Documento creado localmente con DineroMundo.', 105, y, { align: 'center' });
    doc.save(`${cleanFile(`cotizacion-${quote.quoteNumber}`)}.pdf`);
  }
  return { generate, cleanFile };
}));
