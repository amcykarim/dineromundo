(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.DMAutomationTemplates=api;}(typeof self!=='undefined'?self:this,function(){'use strict';
  const definitions=Object.freeze({invoice_due_soon:{name:'Factura próxima a vencer',description:'Avisarme antes de que venza una factura',options:[1,3,7],configKey:'daysBefore',defaultValue:3},invoice_overdue:{name:'Factura vencida',description:'Avisarme cuando una factura esté vencida'},quote_followup:{name:'Seguimiento de cotización',description:'Recordarme dar seguimiento a una cotización',options:[2,3,5,7],configKey:'daysAfter',defaultValue:3},expense_reminder:{name:'Gasto pendiente',description:'Recordarme un gasto pendiente'},income_reminder:{name:'Ingreso pendiente',description:'Recordarme un ingreso pendiente'}});
  const uid=()=>`rule-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,10)}`;
  function create(type,overrides={}){const def=definitions[type];if(!def)throw new Error('Tipo de automatización no válido.');const now=new Date().toISOString(),value=def.configKey?{[def.configKey]:def.defaultValue}:{};return {id:uid(),businessId:'',type,name:def.name,enabled:false,triggerType:'condition',triggerConfig:value,actionType:'in_app_reminder',actionConfig:{},createdAt:now,updatedAt:now,lastEvaluatedAt:null,...overrides};}
  return {definitions,create};
}));
